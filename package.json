{
  "scripts": {
    "dev": "NODE_ENV=development tsx server/index.ts",
    "build": "vite build",
    "build:server": "esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist-server --target=node18",
    "start": "NODE_ENV=production node dist-server/index.js",
    "check": "tsc",
    "db:push": "drizzle-kit push"
  }
}
